{
  "name": "netnavr-pay",
  "version": "0.1.1",
  "private": true,
  "description": "Merchant-owned payment connectivity for the NetNavr runtime.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch src/index.ts",
    "start": "node --env-file-if-exists=.env src/index.ts",
    "test": "node --test tests/*.test.ts"
  }
}
